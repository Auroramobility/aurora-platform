// Central export point for shared, app-wide domain types.
// As Aurora Mobility grows, add feature-specific type files here
// (e.g. `vehicle.ts`, `booking.ts`) and re-export them below.

export type { Database, Json } from "./supabase";
